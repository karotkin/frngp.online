# ===== Production (.env на сервере) =====

# --- Let's Encrypt ---
ACME_EMAIL=admin@frngp.online

# --- вход на сайт (HTTP Basic Auth, теперь поверх HTTPS) ---
BASIC_AUTH_USER=admin
BASIC_AUTH_PASS=ЗАМЕНИ_НА_СТОЙКИЙ_ПАРОЛЬ

# --- курс валют ---
RATE_TTL_MIN=30

# --- postgres (наружу порт не публикуется) ---
POSTGRES_USER=frngp
POSTGRES_PASSWORD=ЗАМЕНИ_НА_СТОЙКИЙ_ПАРОЛЬ_БД
POSTGRES_DB=frngp
